{
  "name": "parkour",
  "private": true,
  "description": "Sitio estático. npm test corre la suite de regresión; el sitio se sirve sin build.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
